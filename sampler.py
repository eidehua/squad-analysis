
out_jsondata = {}
out_jsondata['data'] = []

for i in range(0, len(out_jsondata['data']):
	l = len(jsondata['data'][i]['paragraphs'])
	a = random(0,40)
	b = random(0,40)
	(if a == b)
	  b = a + 1
	curr_paras = [jsondata['data'][i]['paragraphs'][a], jsondata['data'][i]['paragraphs'][b]] 
	out_object = {}
	out_object['paragraphs'] = curr_pars
	out_object['title'] = jsondata['data'][i]['title']
	out_jsondata['data'].append(out_object)